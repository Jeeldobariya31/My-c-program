#include<stdio.h>
void main(){
    int i,j,n;
    printf("enter the size of array:");
    scanf("%d",&j);
    int a[j],b[n];
    for(i=0;i<j;i++){
        printf("enter the value:");
        scanf("%d",&a[i]);
    }
    for
}